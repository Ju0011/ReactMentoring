/*eslint-disable*/
import React, {useState} from 'react';
import Input from 'components/Input';

const Main = (props) => {
	return (
		<>
			<h3>안녕하세요. {values.name} 님!</h3>
		</>
	);
};

export default Main;